/**
 * Objeto de Transferencia de Datos (DTO) que define la forma estandarizada que deben tener los datos
 * al agregar un nuevo padre. Se usa para separar la lógica de validación de la de persistencia.
 */
