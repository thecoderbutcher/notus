/**
 * Acción del lado del servidor para agregar un padre.
 * Esta función valida los datos recibidos, aplica los mapeos necesarios y guarda el padre en la base de datos usando Prisma.
 *
 * Se utiliza desde el formulario de creación (CreateParentForm).
 */
