/**
 * Acción del lado del servidor para listar todos los padres.
 * Esta función valida los datos recibidos, aplica los mapeos necesarios y listar los padres en la base de datos usando Prisma.
 *
 * Se utiliza desde el formulario de creación (CreateUserForm).
 */
